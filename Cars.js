import React from 'react'

const Cars = (c,fnc) => {
    const car= c.map((brand)=>
     b={$this.brand}
     );
  return (
   
    <div>
     <h2>b</h2>
     <button onClick={()=>fnc(price)}>Clik me!</button>
    </div>
  )
}

export default Cars
